# RAT-356 warehouse/KPI execution attempt (2026-05-11)

## Wake context acknowledgment

- Latest comment: `3b477f66-0f5b-43c7-bbdf-ed1ab86295e2` (2026-05-11) routed execution ownership to Data Analyst and requested running warehouse extract + KPI snapshot path.

## Commands executed

```bash
tools/guardrails/check-rat-157-runtime-credentials.sh
tools/guardrails/check-rat-157-warehouse-sql-runtime-path.sh
tools/guardrails/run-rat-311-dashboard-snapshot.sh
```

## Outputs

```text
RAT_157_RUNTIME_CREDS_MISSING:warehouse,bi
RAT_157_WAREHOUSE_SQL_RUNTIME_PATH_MISSING:warehouse_creds
RAT_311_SNAPSHOT_FAIL:missing_warehouse_creds:PGHOST PGPORT PGDATABASE PGUSER PGPASSWORD
```

## Result

- Warehouse extract and KPI snapshot publication path cannot execute in current runtime because warehouse + BI credentials are missing.
- This heartbeat therefore satisfies the board rule to transition issue to `blocked` and name unblock owner/action.

## Unblock owner/action

- Unblock owner: Platform/Infra (runtime secret provisioning) with Data Platform (warehouse surface binding).
- Required action:
  1. Inject runtime warehouse creds (`DATABASE_URL` or full `PG*` tuple).
  2. Inject runtime BI creds (any accepted BI env: `BI_*`/`LOOKER_*`/`METABASE_*`/`SUPERSET_*`).
  3. Bind runtime warehouse to RAT-39 source surface exposing `public.events` and `public.support_tickets`.

## Next action after unblock

- Re-run the three commands above.
- Attach KPI snapshot artifacts (`.csv` + `.md`) generated by the dashboard snapshot script to this issue thread.
